import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Ubuntu HRMS - Human Resource Management System',
  description: 'Ubuntu HRMS: A comprehensive Human Resource Management System for Ubuntu Eco Lodge. Manage employees, attendance, payroll, KPIs, leaves, and contracts with integrated biometric and M-Pesa support.',
  generator: 'v0.app',
  keywords: ['HRMS', 'HR Management', 'Employee Management', 'Attendance', 'Payroll', 'Ubuntu'],
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        {children}
        <Analytics />
      </body>
    </html>
  )
}
